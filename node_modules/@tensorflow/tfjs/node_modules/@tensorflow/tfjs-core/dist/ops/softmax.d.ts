import { Tensor } from '../tensor';
import { TensorLike } from '../types';
export declare class SoftmaxOps {
    static softmax<T extends Tensor>(logits: T | TensorLike, dim?: number): T;
    static softmaxCrossEntropy<T extends Tensor, O extends Tensor>(labels: T | TensorLike, logits: T | TensorLike, dim?: number): O;
}
