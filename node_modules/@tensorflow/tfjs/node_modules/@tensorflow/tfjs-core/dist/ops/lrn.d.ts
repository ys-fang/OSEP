import { Tensor3D, Tensor4D } from '../tensor';
import { TensorLike } from '../types';
export declare class LRNOps {
    static localResponseNormalization<T extends Tensor3D | Tensor4D>(x: T | TensorLike, depthRadius?: number, bias?: number, alpha?: number, beta?: number): T;
}
